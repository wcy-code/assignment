server:
  port: 8762         #开启端口

eureka:
  instance:
    hostname: peer2     #主机名
  client:
    serviceUrl:
      defaultZone: http://peer1:8761/eureka/    #与Eureka Server交互的地址，查询服务和服务注册都需要依赖这个地址